name: MLOX
version: 0.0.1
maintainer: Your Name
description_short: Provides additional functionionality for MLOX.
description: |
  MLOX is a platform that simplifies the deployment and management of machine learning models.
  It provides a user-friendly interface for managing machine learning workflows,
  automating tasks, and integrating with various tools and services.
  This YAML file defines the configuration for the base MLOX stack which is necessary for the
  installation and management of the MLOX platform.
links:
  project: https://github.com/nicococo/mlox/
  news: https://github.com/nicococo/mlox
  security: https://github.com/nicococo/mlox
  documentation: https://github.com/nicococo/mlox
  changelog: https://github.com/nicococo/mlox
requirements:
  cpus: 1.0
  ram_gb: 2.0
  disk_gb: 4.0
build:
  docker: 
    # name: mlox
    # template: docker-compose-mlox-0.0.1.yaml 
    # module: mlox.services.mlox.airflow
    # class_name: MloxDockerService
    # params:
  kubernetes:
  ansible:
