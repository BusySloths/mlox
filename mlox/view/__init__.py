"""Streamlit web UI package (deprecated).

This package backs the Streamlit web UI. It is **deprecated** and will be
moved to a separate plugin repository: the TUI and CLI are the supported
interfaces. Treat anything under ``mlox/view/`` as maintenance-only.

See ``docs/DOCTRINE.md`` for the deprecation decision and timeline.
"""
